# This file is ignored by imp (leading underscore)
# Use it for local secrets or machine-specific overrides that shouldn't be imported
#
# You can manually import this in default.nix if needed:
#   imports = [ ./_secrets.nix ];
{ }
